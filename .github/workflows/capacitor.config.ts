import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.deliveryassistant',
  appName: 'MosaedDriver',
  webDir: 'dist'
};

export default config;
